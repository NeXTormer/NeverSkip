import 'package:flutter/material.dart';

class ExpandingSearchBar extends StatelessWidget {
  const ExpandingSearchBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
